"""Water-safety and drowning data sources.

Sources here emit already-normalized canonical rows from
:mod:`cdata.transforms.drowning` - incident rows (Layer B) or aggregate stat
rows (Layer A), never both, and never raw source columns.
"""
